import { describe, it, expect, vi } from 'vitest'; import { render, screen } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { MatchQuiz } from './MatchQuiz'; describe('MatchQuiz Component', () => { it('renders the question title', () => { render(<MatchQuiz questionId='q1' title='Autonomia do Banco Central' onAnswer={vi.fn()} />); expect(screen.getByText('Autonomia do Banco Central')).toBeInTheDocument(); }); it('calls onAnswer with vote and weight when a button is clicked', async () => { const handleAnswer = vi.fn(); const user = userEvent.setup(); render(<MatchQuiz questionId='q1' title='Autonomia do Banco Central' onAnswer={handleAnswer} />); const btnSim = screen.getByRole('button', { name: /SIM/i }); await user.click(btnSim); expect(handleAnswer).toHaveBeenCalledWith('q1', { vote: 'SIM', weight: 1 }); }); });